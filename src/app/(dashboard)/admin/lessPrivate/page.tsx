import { DashboardPlaceholder } from "@/components/layout/dashboard-placeholder";

export default function AdminLessPrivatePage() {
    return (
        <DashboardPlaceholder
        eyebrow="Learning Program"
        role="ADMIN"
        title="Private Lesson"
        description="Manage private lesson packages, schedules, and learning access for students."
        />
    );
}
